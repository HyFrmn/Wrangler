<h3>Listing Tasks</h3>

<table>
  <thead>
	<tr>
	  <th>Environment</th>
      <th>Priority</th>
      <th>Command</th>
      <th>Status</th>
      <th>Job</th>
    </tr>
  </thead>

<% @tasks.each do |task| %>
  <%= link_to (render :partial => 'tasks/row', :locals => { :task => task }), task %>
<% end %>
</table>